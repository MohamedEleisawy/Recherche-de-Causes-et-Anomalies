# version: "3.8" obsolete
services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: taskdb
      POSTGRES_USER: taskuser
      POSTGRES_PASSWORD: taskpass
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"

  # Erreur trouvée : Redis nécessite un backend lancé, et pour lancer le backend il faut que redis soit lancé. Donc problème
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: "postgres://taskuser:taskpass@db:5432/taskdb"
      REDIS_URL: "redis://redis:6379"
    depends_on:
      - db
      - redis
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "3000:8080"
    depends_on:
      - backend
    volumes:
      - ./frontend:/app

volumes:
  postgres_data:
